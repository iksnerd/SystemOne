# Only needed once a Gemini teacher backend exists (labeling stage). Never commit real values.
GEMINI_API_KEY=
# Local Ollama, used only to generate synthetic states, never to label them.
OLLAMA_URL=http://localhost:11434
