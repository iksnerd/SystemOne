---
type: llm
---

Pass if the response classifies it as a fix from reading the one message; a brief note on when it could be read otherwise is fine. Fail if it sets up verdict, a model, or any tooling to classify a single message.
